# Galweave v5.0.1

v5.0.1 是性能优化版：把大文件场景下最卡顿的两处重活（全文搜索、格式识别）移出主线程，并收敛主线程上的 O(n) 重算。

## 主要更新

### 搜索 Off-main-thread

- 全文匹配 / 匹配计数 / 全部替换 / 编号跳转全部移入 Web Worker。
- 搜索框输入防抖 180ms，不再每个按键同步扫整篇文本。
- 替换与跳转结果经 Worker 返回；Worker 不可用（如单文件 HTML 在 file:// 下打开）时自动降级为同步路径，功能不受影响。

### 识别 Off-main-thread

- 格式检测 / 规范化 / 还原 / 编辑器模拟移入 Web Worker。
- 「识别」按钮点击后界面不再卡顿，报告异步返回。

### 主线程 O(n) 收敛

- 人名列宽计算加缓存，仅结构性变更（打开文件、批量替换、恢复进度）时重测。
- 搜索命中高亮改用 Set 索引：O(挂载行 × 匹配数) → O(挂载行)。

### 验证

- 全量 128 项单元测试通过。
- 浏览器实测 5000 行文件：搜索「魔法」1/5000 处命中、F3 跳转、行高亮均正常；识别报告含编辑器模拟无损还原 ✓。

## 下载说明

- `Galweave_5.0.1_x64-setup.exe`：Windows NSIS 安装器（推荐）。
- `Galweave_5.0.1_x64_en-US.msi`：Windows MSI 安装包。
- `Galweave_5.0.1_portable.exe`：免安装可执行文件。
- `Galweave_5.0.1_single.html`：浏览器单文件版（本地 Sakura 机翻不可用，其余核心功能可用）。

## 升级说明

- 直接安装覆盖旧版本或使用新的免安装 exe。
- 项目目录中的 `glossary.json`、`snippets.json`、`*.progress.json` 和 `*.proof.json` 不会被安装程序删除。
