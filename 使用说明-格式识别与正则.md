# 格式识别与正则处理文本 — 使用说明与原理

本文面向使用本工具（桌面 exe / 单文件 HTML / CLI 脚本）的用户，讲解两套"按格式处理文本"的手段：

1. **文本格式识别**（「格式/规则」弹窗 ①自动识别 + `scripts/recognize-format.mjs`）——自动分析文件格式并转换；
2. **自定义前缀正则**（「格式/规则」弹窗 ②手动规则）——手工用正则描述行的结构。

它们解决同一个问题：**不同 galgame 引擎导出的文本结构五花八门，工具默认只认 `☆/★` 一种，需要先"告诉"工具你的文件长什么样**。

---

## 一、先理解工具的底层模型

无论什么格式，工具都按同一个模型处理文本：

```
文件 = 若干"段落"(段落之间用空行隔开)
段落 = 原文行 [+ 译文行]
行   = 前缀 + 正文
前缀 = 标记字符 + 编号 [+ 标记 + 说话人 + 标记] ...
```

例（默认格式）：

```
☆0003☆桐吾☆「ありがとう」     ← 原文行: 前缀=☆0003☆桐吾☆  正文=「ありがとう」
★0003★桐吾★「谢谢」           ← 译文行: 前缀=★0003★桐吾★  正文=「谢谢」
```

- **标记字符**：原文行用 `☆`，译文行用 `★`（可换，如 `○/●`）。
- **编号**：段内第一段，如 `0003`、`TEXT|0`、`00000|000008|003`。
- **说话人**：第二段，如 `桐吾`；没有则为空（`☆0002☆☆正文`）。
- **NAME 行**：编号以 `NAME` 开头（如 `☆NAME|7☆【宗一郎】`），正文即人名。

工具解析时：把行按标记字符切开 → 第 1 段是编号、第 2 段是说话人、最后一段是正文。

**识别不出的后果**：如果标记不对（默认 ☆/★ 但你的文件是 ○/●），整行会被当成"没有前缀的正文"，编号和说话人全部丢失，翻译进度也无法按行保存。

---

## 二、文本格式识别（推荐，自动）

### 2.1 原理：识别脚本在做什么

识别脚本（`src/recognize.js`，GUI 与 CLI 共用同一份逻辑）按四步自动分析：

**① 标记字符检测（markerStats + detectMarks）**
- 扫描每行开头，找出"像标记"的字符：出现在行首、且在同一行里再次出现（形成 `标记…标记…` 前缀结构）的符号，如 `☆`、`○`、`[` 等；文字/数字开头的不算。
- 统计每个候选标记的行数，并收集每种标记行里的"编号"（标记与标记之间的内容）。
- **成对匹配**：如果 A 标记行的编号集合与 B 标记行的编号集合大量重合（同一段落里 `○0003○…` 配 `●0003●…`），就判定 A 是原文标记、B 是译文标记。重合越多，置信度越高。

**② 成对分块（makePairBlocks）**
- 按空行把文件切成段落；每个段落里找"以原文标记开头的行"和"以译文标记开头的行"。
- 用标记字符切开行 → 得到 `编号 / 说话人 / 正文`；正文前部的 `[[名字]]` 会被识别为说话人（如第二种.txt）。
- 段落里的 `#NOTTRANS` 等注释行被记为"控制行"，附着在该行上原样保留。

**③ 结构分析（buildProfile）**
- 编号形状：纯数字 / 十六进制 / `|` 管道 / `TEXT|n` / `NRT` 后缀；
- 说话人来源：前缀段 / 正文 `[[ ]]` / 无；
- 行分类：对话（以「开头）/ 旁白 / 有名文本 / 控制行；
- 正文标签统计：`[np]`、`[r]`、`<r>`、`%p…;%f…;` 等；
- 问题清单：原文/译文编号不一致、译文独有、原文独有、无法归类行等。

**④ 生成档案 + 转换**
- 档案（profile JSON）：上面全部分析结果 + `parseConfig`（识别出的 open/close）+ 逐行结构。
- **规范化（canonicalize）**：按档案把整个文件重写成工具原生 `☆/★` 两行格式——说话人搬进名字栏；
  **对话行正文只保留 `「…」` 内内容，`「」` 外的标签（`[np]`/`[r]`/`<r>`/`%p…;%f…;`）不进入译文编辑区**
  （记录在档案的 `sourceExtra/translationExtra`），旁白行（无「」）标签原样保留——这就是"生成格式后导入编辑器"。
- **还原（restore）**：翻译完，用档案把 `☆/★` 规范化文本**逐行换回原格式**（编号、说话人位置、`[[名字]]` 样式、
  以及被剥离的括号外标签全部复原），保证游戏引擎能读。

### 2.2 GUI 用法（exe 与 HTML 都有）

1. 顶部点 **「格式识别」**；
2. 选来源：**当前已打开文件**（自动用原始文本）/ **粘贴或选择文本**；
3. 点 **「识别」**，看报告：
   - 标记字符（原文 ○ / 译文 ●）、置信度；
   - 结构：编号形状、说话人来源（前缀段 / [[名字]]）、行分类统计；
   - 问题清单（编号错位等）；
   - 底部"编辑器模拟"：用真实解析器对比"原文件+识别配置"和"规范化文本"的导入效果（有编号数 / 名字栏可用数 / 无损还原）；
4. 三个动作任选：
   - **「应用为解析规则」**：把识别出的 open/close 保存为解析规则（重新导入原文件生效，格式不变）；
   - **「规范化并载入编辑器」**：直接转成 `☆/★` 载入，名字栏可用，立即开翻（推荐）；
   - **「下载 profile JSON / 规范化文本」**：保存档案和规范文本，翻译完用 CLI `--restore` 还原。

### 2.3 CLI 用法

```bash
# 1) 看识别报告
node scripts/recognize-format.mjs "test text/第二种.txt"

# 2) 保存识别档案
node scripts/recognize-format.mjs "xxx.txt" --json 档案.json

# 3) 规范化成 ☆/★(默认输出 xxx.canonical.txt)
node scripts/recognize-format.mjs "xxx.txt" --convert

# 4) 翻译完还原原格式(只需 --profile,输出默认 xxx.canonical.txt → xxx.restored.txt)
node scripts/recognize-format.mjs "xxx.canonical.txt" --profile 档案.json

# 5) 旧编码文件
node scripts/recognize-format.mjs "old.txt" --encoding shift_jis
```

### 2.4 典型工作流

| 文件情况 | 处理方式 |
|---|---|
| 标准 `☆/★` | 直接导入，无需识别 |
| 只是换了标记（`○/●`、`@/#`） | 「格式/规则」识别 →「应用为解析规则」→ 重新导入 |
| `[[名字]]` 说话人、N 后缀名字行、`#NOTTRANS`、编号错位等复杂格式 | 格式识别 →「规范化并载入编辑器」→ 翻译 → 导出/`--restore` 还原 |

---

## 三、自定义前缀正则（「格式/规则」弹窗 ②手动规则）

### 3.1 原理：正则如何描述一行

"规则"的本质是：**用一条正则把一行拆成 前缀 + 正文 两段**。

- **第 1 个捕获组 `(...)` = 前缀**（标记 + 编号 + 说话人）；
- **第 2 个捕获组 `(...)` = 正文**；
- 前缀会被**再按标记字符切开**：第 1 段=编号，第 2 段=说话人。

规则只有三个字段：

| 字段 | 含义 |
|---|---|
| 原文标记字符 | 原文行的标记（默认 ☆），同时用于拆前缀和导出 |
| 译文标记字符 | 译文行的标记（默认 ★） |
| 自定义前缀正则 | 留空 = 按标记自动生成标准结构；填写则完全用它 |

**留空时的自动正则**（这就是"标准结构"的原理）：

```
^([标记][^标记]*[标记](?:[^标记]*[标记])?)([\s\S]*)$
```

用 `☆/★` 代入即：

```
^([☆★][^☆★]*[☆★](?:[^☆★]*[☆★])?)([\s\S]*)$
```

- `[☆★]`：匹配一个标记字符；
- `[^☆★]*`：匹配任意个"非标记"字符（编号/说话人）；
- `(?:…)?`：第三段（说话人后的标记）可有可无——所以 `☆0003☆桐吾☆正文` 和 `☆TEXT|0☆正文` 都能拆；
- `([\s\S]*)`：第 2 组匹配"任意字符（含换行）"作为正文。

以 `☆0003☆桐吾☆「ありがとう」` 为例：
- 第 1 组捕获 `☆0003☆桐吾☆` → 前缀；
- 把前缀里的 `☆` 替换成换行再分段 → `['', '0003', '桐吾', '']` → 编号=`0003`，说话人=`桐吾`；
- 第 2 组捕获 `「ありがとう」` → 正文。

### 3.2 什么时候需要自定义正则

只有"标准三段结构"套不上时才需要，例如行是 `<编号>[正文]` 这种与标记无关的结构：

```
<0009>[こんにちは]
```

在「格式/规则」弹窗 ②手动规则填：

```
^<(.+?)>\[(.+)\]$
```

- 第 1 组 `(.+?)` 捕获 `0009` → 编号；
- 第 2 组 `(.+)\]` 捕获 `こんにちは` → 正文。

用弹窗里的**测试框**可以实时验证拆分结果（显示"前缀 / 正文"）。

### 3.3 常用正则速查

| 用途 | 写法 | 说明 |
|---|---|---|
| 任意单个字符 | `.` | 换行除外 |
| 任意字符（含换行） | `[\s\S]` | 正文常用 |
| 数字 | `\d` 或 `[0-9]` | 编号 |
| 非某字符 | `[^☆★]` | 标记之间的内容 |
| 可选段 | `(?:…)?` | 可有可无 |
| 捕获组 | `(...)` | 第 1 组=前缀、第 2 组=正文（契约！） |
| 行首/行尾 | `^` / `$` | 锚定 |
| 转义特殊字符 | `\` | `. * + ? ( ) [ ] { } ^ $ \|` 都要转义 |

**契约（务必遵守）**：
1. 正则必须含 **2 个捕获组**：第 1 组=前缀，第 2 组=正文；
2. 前缀会被按标记字符切开取 编号/说话人，所以第 1 组里**不要**把说话人和编号写死成一段；
3. 正则**匹配整行**（一般以 `^` 开头、`$` 结尾），没匹配上的行会整体当正文。

### 3.4 正则 vs 格式识别的取舍

| | 自定义正则 | 格式识别 + 规范化 |
|---|---|---|
| 适合 | 结构简单、规则明确、只有标记/编号不同 | 说话人在 `[[ ]]`、N 后缀名字、注释行、编号错位等复杂结构 |
| 标记字符 | 必须自己知道 | 自动检测 |
| 名字栏 | 前缀段才有名字栏 | 规范化后 `[[名字]]` 也能进名字栏 |
| 还原原格式 | 不支持（导出固定 ☆/★） | 支持（`--restore` 按档案还原） |
| 上手难度 | 要懂正则 | 点两下 |

**建议**：先试「格式识别」——它能在 5 秒内告诉你标记、编号、说话人在哪、有没有问题；正则适合你非常确定格式、且只想微调标记或加一条简单规则时用。

---

## 四、常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 导入后行没有编号，全是整行正文 | 标记字符不对（默认 ☆/★ 但文件是 ○/● 等） | 「格式/规则」识别 →「应用为解析规则」→ 重新导入 |
| 说话人显示不出来、名字栏空白 | 名字在正文前部 `[[名字]]` 里 | 格式识别 →「规范化并载入编辑器」 |
| `#NOTTRANS` 之类被当成正文行 | 注释行与正文同段 | 已修复：编辑器自动剥离注释行并附着到段落（`p.comments`），正文正常提取，导出还原回原位 |
| 原文和译文行对不上 | 文件里 ★ 行编号错位 | 报告会列出编号不一致，核对源文件 |
| 测试框拆分结果不对 | 正则捕获组不对 | 检查是否 2 组、是否匹配整行、特殊字符是否转义 |

**编号后缀含义**（格式识别已按此处理）：`T` = TEXT 文本行（对话/旁白，按内容分类）、`N` = 名字行（`000001N` 等，识别为名字条目，名字栏可编辑）、`R` 等为其他控制行；十六进制编号（如 `0000000A`）不当作后缀处理。
**名字包裹样式**：`[[名]]`（如第二种.txt）与 `【名】`（如 `☆NAME|7☆【宗一郎】`）都会被识别为说话人——名字取括号内、编辑器名字栏显示纯净名字，导出/还原时自动按原样式还原括号。
