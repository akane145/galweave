{
  "name": "galweave",
  "private": true,
  "version": "5.3.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:no-mt": "vite build --mode no-mt",
    "build:html": "vite build --mode single",
    "preview": "vite preview",
    "test": "node --test \"tests/*.test.mjs\"",
    "lint": "eslint .",
    "tauri": "tauri",
    "tauri:build:no-mt": "tauri build --config src-tauri/tauri.no-mt.conf.json && node scripts/copy-no-mt-artifacts.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "vite": "^8.2.1",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.2",
    "@tauri-apps/plugin-store": "^2.4.4",
    "buffer": "^6.0.3",
    "js-mdict": "^6.0.8",
    "pako": "^3.0.1"
  }
}
