# Galweave 使用教程

本教程适用于 Windows 桌面版 `galweave.exe` 和单文件浏览器版 `Galgame翻译工具-浏览器版.html`。工具用于处理按空行分段、以 `☆` 表示原文、以 `★` 表示译文的视觉小说脚本文本。

> 重要：保存或导出的文本会写成 UTF-8，不保留原文件的 Shift-JIS、EUC-JP 或 UTF-16 编码。第一次处理项目时请使用副本，并在保存后确认游戏引擎能正常读取。

## 1. 选择版本

| 场景 | 建议版本 |
|---|---|
| 需要旧日文编码、真实项目目录术语表、`.proof.json` | 桌面版 |
| 只需快速编辑 UTF-8 文本，不想安装 | 浏览器版（Chrome/Edge） |
| 需要直接写回浏览器中打开的文件 | Chrome/Edge，并在文件选择时授权 |
| 使用 Firefox 等不支持 File System Access API 的浏览器 | 可以编辑，但保存通常会另存或下载 |

两种版本的翻译进度不互通。建议同一个项目固定使用一种版本。

## 2. 快速开始

1. 先复制一份待翻译文件作为测试副本。
2. 桌面版双击 `galweave.exe`；浏览器版用最新版 Chrome 或 Edge 打开 HTML。
3. 点击“导入文本”选择 `.txt` 或 `.ks`，也可把文件拖入窗口。
4. 如果使用同目录多个文件，点击“侧边栏”并选择文件夹，可从目录树切换。
5. 在译文框输入中文；`Tab` 或 `Enter` 到下一行，`Shift+Tab` 到上一行。
6. 工具会在约 250ms 停止输入后保存翻译进度，但这不等于已经写回原文件。
7. 完成后使用“保存原文件”写回，或使用“导出译文”生成副本。
8. 保存后用文本编辑器和游戏引擎检查编码、标记和脚本标签。

桌面版写回原文件时，会把保存前的当前文件覆盖为同路径 `.bak`，即 `.bak` 始终是上一次保存的内容。该备份不是绝对保证，重要文件仍应自行复制。

## 3. 文本格式

### 3.1 段落规则

文件以空行分段。每段预期为：

- 第 1 行：原文。
- 第 2 行：可选的已有译文。
- 段落之间：至少一个空行。

```text
☆0003☆张三☆「ありがとうみんな！」
★0003★张三★「谢谢大家！」

☆TEXT|0☆天気がいいから、散歩しましょう。。
★TEXT|0★天气很好，所以去散步吧。

☆NAME|11☆李四
★NAME|11★李四
```

以上是默认「标准 ☆/★」格式。其他引擎格式（如 ○/●、固定行数分组、`#` 指令行、`[[名字]]`/`【名字】` 说话人）请在顶部“规则”里切换到对应**格式预设**，或粘贴示例自动识别（见第 9 章）。

### 3.2 支持的行形态

| 形态 | 示例 | 说明 |
|---|---|---|
| 说话人 | `☆0003☆张三☆正文` | 编号、原始人名、正文 |
| 空说话人旁白 | `☆0002☆☆正文` | 名字段为空 |
| 普通旁白 | `☆TEXT|0☆正文` | 只有编号与正文 |
| NAME 条目 | `☆NAME|11☆李四` | 编号以 `NAME` 开头，正文作为人名 |

`NAME` 行只要译名框非空就计为已翻译；即使仍是原名也会输出对应的 `★` 行。普通行只有译文正文非空时才计为已翻译并输出译文行。

### 3.3 锁定括号

当整段正文首尾完整包裹 `「」` 时，工具会把括号显示在输入框外。你只编辑中间内容，导出时再组合为一对括号。

该功能只识别整段首尾，不解析正文内部的嵌套引号。

### 3.4 脚本标签

`%p-1;`、`%f...;`、转义文本等内容会作为普通正文显示。工具不会理解游戏引擎指令，请在翻译时自行保持标签完整。

## 4. 界面与逐行编辑

顶部区域包含文件操作、规则、主题、校对和侧栏；第二行包含搜索、替换、范围、跳转、撤销重做。

每个段落包含：

- 行号和原文编号。
- 原始人名，只读。
- 原文正文，只读。
- 译名输入框。
- 译文 textarea。
- 复制原文按钮。
- 校对模式开启时的批注、通过和问题按钮。

### 4.1 输入框行为

- textarea 会按内容软换行并自动增高。
- 高度最高为 400px，超过后在输入框内部滚动。
- `Enter` 用于进入下一行，不会在译文内插入换行。
- `Tab` 到下一行，`Shift+Tab` 到上一行。
- 同一行约 800ms 内的连续输入会合并为一份撤销快照。
- 撤销栈最多保留 100 步。

### 4.2 进度与字符统计

“已翻译 N / 总数”按完成段落数计算。字符统计排除 `NAME` 行，显示译文字符数与原文字符数的比值；译文比原文长时百分比可能超过 100%，它不是严格的句子覆盖率。

### 4.3 人名编辑

带说话人的段落会显示原始人名和可编辑译名。修改译名后：

- 导出时写入译文前缀。
- 不同于原名的译名会沉淀到人名表。
- 之后载入文件时，如果译名仍等于原名，人名表可以自动填入译名。
- 人工改过的译名不会被自动应用覆盖。

该行为桌面版和浏览器版都支持，但两者的术语表存储位置不同。

### 4.4 段落右键菜单

在段落行的**非输入区**（行号列、原文列、行空白处）点右键，会弹出该行的快捷菜单：
复制原文到译文 / 复制原文到剪贴板 / 套用翻译记忆 / 机翻本行 / 清空本行译文 / 标记通过 / 标记有问题 / 添加批注 / 历史版本… / 撤销。
不可用的项会置灰（如未开启校对模式时三项校对动作、`NAME` 行不能机翻、无译文时不能清空、翻译记忆里没有该行可用条目时「套用记忆」置灰）。

其中两项值得单独说：

- **套用翻译记忆**：翻译记忆里命中该行时，菜单项会直接显示建议译文（`套用记忆：早上好`）。点一下即写入，可 `Ctrl+Z` 撤销。精确命中优先；只有相似命中时也会给出，但**是否采用由你判断** —— 相似不等于语义相同。
- **历史版本…**：见 [§11.4](#114-历史版本本地留档)。

**译文 textarea 上不接管右键**：在译名或译文框里右键仍是浏览器原生菜单，粘贴、输入法候选、拼写检查全部照旧。
菜单靠近屏幕边缘时会自动向内翻转，不会跑出视口；按 `Esc`、点击菜单外部、或滚动/改变窗口尺寸都会关闭它。

## 5. 导入、目录侧栏和文件切换

### 5.1 单文件导入

“导入文本”打开文件选择器。桌面版使用系统对话框；Chrome/Edge 浏览器版会优先请求文件句柄，获得授权后可以写回同一文件。

拖拽支持 `.txt`、`.ks`、`.ks.txt`。浏览器普通文件选择或拖拽在部分环境中只提供一次性文件内容，不保证后续可以原地写回。

### 5.2 文件夹侧栏

点击“侧边栏”后选择文件夹。目录树按目录优先排序，可展开子目录并点击文件。工具不会记住上次选择的目录。

桌面版目录树拥有真实路径，文件切换时可以使用项目术语表和独立 `.proof.json`。浏览器版即使通过目录选择器打开文件，术语表仍使用浏览器本地存储，不会在目录中创建 `glossary.json`。

目录树可能显示非 `.txt/.ks` 文件；点击这些文件会按文本尝试读取。不要用它打开二进制文件。

### 5.3 切换文件时发生什么

- 当前校对脏数据会先尝试保存。
- 新文件重新解析。
- 如果存在翻译进度，会提示是否恢复。
- 恢复采用合并：新文件自身已有译文优先，缓存只补空缺。
- 桌面版加载项目术语表和该文件的 `.proof.json`。
- 人名表自动应用到尚未人工修改的译名。

## 6. 搜索、替换和跳转

### 6.1 搜索范围

| 范围 | 内容 |
|---|---|
| 原文+译文 | 原文正文和译文，不包含名字 |
| 仅原文 | 仅原文正文，不能替换 |
| 仅译文 | 仅译文 |
| 仅名字 | 仅可编辑译名 |

搜索是普通子串匹配，不支持正则。可选择区分大小写。

### 6.2 导航和替换

- 在搜索框按 `Enter` 到下一匹配，`Shift+Enter` 到上一匹配。
- 也可使用“上一个”“下一个”或 `F3` / `Shift+F3`。
- 原文命中会高亮；名字命中会标记译名框。
- “替换”或 `Ctrl+Enter` 替换当前译文/名字匹配。
- “全部替换”先显示匹配数量并请求确认。
- 原文始终只读，搜索到原文时不能替换。

如果在搜索后手动修改了译文，工具会在替换前校验匹配位置并重新计算，避免使用旧坐标修改错误字符。

### 6.3 编号跳转

在跳转框输入：

- 数字，例如 `3`：匹配编号末尾数字为 3 的第一段。
- 完整编号，例如 `TEXT|0`、`NAME|11`：忽略大小写匹配。

`F2` 跳到下一个未翻译段落，到文件末尾后从头循环。

### 6.4 撤销和重做

- `Ctrl+Z`：撤销。
- `Ctrl+Y`：重做。
- 批量替换、清空、术语应用会按批次进入撤销栈。
- 校对修改记录中的“还原”也可用 `Ctrl+Z` 撤销。

## 7. 术语表和人名表

### 7.1 存储方式

**桌面版：**

1. 优先使用当前源文件目录的 `glossary.json`。
2. 项目文件不存在或不可用时，使用 EXE 同目录的全局 `glossary.json`。
3. 同目录文件共享一个项目术语表。

**浏览器版：**

- 使用浏览器本地存储中的全局术语表。
- 不会在所选文件夹中写 `glossary.json`。
- 清除浏览器站点数据后可能丢失，应定期导出 JSON。

### 7.2 人名表

人名表保存“原名 → 译名”。打开文件时，只对仍未人工修改的译名自动应用。输入不同于原名的译名后，会自动记录或更新该条目。

### 7.3 词条表

词条命中：

- 区分大小写。
- 长词条优先，避免短词抢占长词的一部分。
- 在原文中显示高亮。
- 点击高亮会把词条译文追加到当前行译文末尾。

“应用”会在已有译文中查找词条原文并替换为译文：

- 不修改原文。
- 跳过空译文。
- 跳过 `NAME` 行。
- 执行前会确认，并可撤销。

### 7.4 导入导出

术语面板支持 JSON 导入和导出。建议在大批量编辑前导出备份。导入 JSON 会替换当前内存术语数据，不要把不可信或格式错误的 JSON 直接导入正式项目。

### 7.5 翻译记忆（TM）

术语表做的是**词**级替换，翻译记忆做的是**句**级复用 —— galgame 里重复句式极多，同一句话翻译过一遍就不该再花钱翻第二遍。

**它怎么工作**

1. **自动入库**：每次「保存原文件」或「批量翻译」之后，所有已完成的行（源句 → 译文）会被收割进记忆库。源句过短（<2 字，如「はい」）的不收 —— 复用价值低且容易误套。
2. **批量机翻先查库**：点「批量翻译」时，先对未翻行做精确匹配。
   - **精确命中**直接套用，**不调用机翻**（确认框里会写明"已有 N 行由翻译记忆直接套用"）。
   - **相似命中**不自动套用，只在确认框里提示"另有 M 行存在相似记忆"。是否采用由你判断 —— 相似 ≠ 语义相同。
3. **逐行套用**：在行上点右键 →「套用记忆：<建议译文>」。精确命中优先，否则用相似度最高的一条（阈值 0.75，实测标定：只差一个语气的句子约 0.82，同句式换主语约 0.5–0.7，无关句低于 0.2）。套用后可 `Ctrl+Z` 撤销。

**存储位置**：桌面版存在源文件同目录的 `.galweave/tm.json`，**随项目走**（同目录多个文件共享一份）；浏览器版存在 IndexedDB。最多保留 20000 条，超出按最后使用时间裁剪。

**注意**

- 记忆库是"加速器"不是"权威"：读不出来时静默当作空库，绝不阻断翻译流程。
- 项目目录不可写时记忆不会落盘（当次会话内仍可用）。
- 换机器 / 换目录后记忆库不会跟着走，需要连同 `.galweave` 目录一起拷。

## 8. 校对模式

点击顶部“校对”进入校对工作流。校对模式提供状态、批注和修改记录。

### 8.1 状态

| 状态 | 含义 |
|---|---|
| 待校对 | 尚未确认 |
| 有问题 | 需要修改 |
| 已通过 | 当前文本已确认 |

使用行内 `✓` 和 `⚠` 切换状态，再点一次可回到待校对。顶部校对栏可按状态过滤，并显示总数。

联动规则：

- 新增未解决的“问题”或“疑问”批注时自动变为有问题。
- 相关问题/疑问全部解决或删除后，从有问题回到待校对。
- 已通过行再次编辑、撤销、重做或还原时，旧通过状态失效。

### 8.2 批注

点击行内 `📝`，选择类型并输入内容：

- 问题
- 建议
- 疑问
- 备注

批注可以解决或删除。侧栏“校对 → 批注”集中显示，支持跳到对应行。左侧徽标显示该行未解决批注数。

“下一问题”会优先寻找有未解决批注的行，然后寻找状态为有问题的行；建议和备注只要未解决，也可能被该功能定位。

### 8.3 修改记录

只有校对模式开启时才记录修改：

- 普通输入在停止约 1200ms 或离开输入框时按整句生成一条记录。
- 替换、撤销、重做、还原和批量操作也会记录。
- 完整保留并展示所有修改对比。同一行同一字段只保留首次修改前与最新文本；改回最初文本后移除记录。
- 可跳到对应行，或用“还原”恢复修改前值。
- “还原”会进入撤销栈。

桌面版将状态、批注和修改记录统一保存到 `<源目录>/.galweave/<文件名>.proof.json`。浏览器版保存到 IndexedDB。“清除进度”会连带删除当前文件的校对数据。

### 8.4 校对快捷键

代码默认值：

| 动作 | 默认键 |
|---|---|
| 通过并下一行 | `Q` |
| 标记有问题 | `W` |
| 打开批注 | `A` |
| 下一问题 | 未设置 |
| 开关校对 | 未设置 |

在“校对 → 快捷键”中可以重新录制。单键在译文/译名框聚焦时可能与输入拉丁字母冲突；需要输入英文时建议改为组合键。发布包里的 `settings.json` 可能已经带有自定义键位，实际设置以界面为准。

### 8.5 漏翻 / 异常 / 一致性清单

侧栏「校对 → 漏翻/异常」把**单行完成度**与**跨行一致性**两类问题合并成一张清单，点任意一条即跳到该行。顶栏「漏翻」计数与清单里的数字始终一致。

**单行完成度**（不依赖术语表）：

| 标签 | 含义 |
|---|---|
| 漏翻 | 译文为空 |
| 占位未译 | 译文与原文完全相同（照抄占位） |
| 长度可疑 | 译文/原文长度比越界（默认 0.3–3.5） |

**跨行一致性**（需要术语表；术语表为空时不会产生这类条目）：

| 标签 | 含义 | 判据 |
|---|---|---|
| 术语未用 | 源句命中了词条，译文却没出现约定译名 | 单字词条不检查；每行最多报 3 条，长词条优先 |
| 译名冲突 | 同一源名在本篇出现多个不同译名 | 每个冲突名只报首次出现行一条，并列出各译名及其次数 |
| 占位符缺失 | 源文里的数字或 `【…】` 内容在译文中找不到 | 数字按跨全半角比对（`３` 与 `3` 视为同一个）；`【】` 内超 20 字不算占位符 |
| 半角标点 | 半角标点紧贴中日文，或 `「」` 不配对 | **只在紧贴中日文时报**：`Sakura 3.0`、`CV: xxx` 这类拉丁语境不会误报 |

注意：**完全没译文、或照抄原文的行只归“漏翻/占位未译”报一次**，不会因为术语表再报一条 —— 清单是给人看的，同一行重复出现两条会让人放弃它。

因此这里的判定都是**启发式**，取向是**宁漏报不误报**：只提示"值得看一眼"，不替你做决定。

## 9. 解析规则（格式预设）

顶部“规则”打开**格式预设**面板，让工具适配不同游戏引擎的文本结构。

### 9.1 内置预设

| 预设 | 适用格式 |
|---|---|
| 标准 ☆/★ | 空行分隔，原文+译文两行，说话人内联 |
| ☆/★ + 指令行 | 元信息行（如 `#NOTTRANS`）+ 原文 + 译文 |
| ○/● + [[名字]] | 说话人在正文 `[[名]]` 中，行尾 `[np]` 等标记原样保留 |
| ☆/★ + N 后缀名字 | 编号以 `N` 结尾判为名字条目 |
| ☆/★ + 【名字】 | 说话人在正文 `【名】` 中 |
| 固定行数分组 | 无空行，每 N 行一组（支持 3/4 行组） |

选择预设即自动填入各字段，可再微调。

### 9.2 可配置项

- **原文/译文标记字符**：默认 `☆/★`，可改如 `@/#`、`○/●`。
- **分组方式**：空行分组（段落间空行）或固定行数分组（每 N 行一组，无空行）。
- **名字来源**：内联段（`☆名字☆`）或正文包裹（`[[名字]]`、`【名字】`）。
- **名字条目判定**：编号以 `NAME` 开头 / 编号以某后缀结尾（如 `N`）/ 无名字条目。
- **元信息行前缀**：以这些字符开头的行原样保留不翻译（如 `#`）。
- **自定义前缀正则**：第 1 捕获组=前缀、第 2 捕获组=正文；留空按标记自动生成。

### 9.3 从示例识别

在示例文本框粘贴一段真实文本，点 **「从示例识别」** 自动推断上述规则，右侧实时预览解析结果。识别是启发式推断，个别文件可能不准，可在面板里手动微调后用预览验证。

### 9.4 导入时自动识别

勾选 **「导入文本时自动识别格式」** 后，每次导入会取文件头部样本自动推断格式（结果仅覆盖本次解析）；不勾选则始终使用面板里保存的格式。适合混合导入多种格式文件的场景。

保存后重新导入文件生效。

## 10. 主题、背景、字体和侧栏

### 10.1 主题

工具栏「主题」打开统一设置弹窗：**深色 / 浅色 / 黑白**三种模式（黑白为灰阶配色，背景图也会变灰）。旁边 🌓 按钮在三种模式间循环快速切换，无需打开弹窗。选择保存在本机设置（桌面版 `settings.json` 的 `ui.mode`，旧版 localStorage 主题首次启动自动迁移）。

### 10.2 背景

“主题”弹窗内“背景图片”支持 JPEG、PNG、WebP、GIF，文件上限约 4MB。可设置遮罩透明度和显示方式：铺满裁剪、完整显示、拉伸、平铺、原始大小居中。

**遮罩强度**压的是整张背景图（数值越高背景越收敛、文字越清晰）；**文本区透明度**决定对白行本身让出多少（0% = 实色，默认；数值越高背景图越明显）。两者互补，配合着调即可在“看得见场景”和“读得清文字”之间取平衡。未设置背景图时这一项看不出效果。

桌面版背景以 data URL 存入 EXE 同目录 `settings.json`，可能显著增大该文件。对外发布前检查是否需要保留背景数据。

### 10.3 原文/译文字体

“主题”弹窗内可分别设置**原文**与**译文**的字体（留空=跟随主题默认）、字号（8–72）与颜色，保存后立即生效，随设置持久化。

### 10.4 自定义字体（导入文件 / 指定字体文件夹）

「文本排印」页的 **02 自定义字体**区块（与「01 对白排印」「03 字数校验口径」同级），用于引入系统里没装的字体（例如某部作品的专用字体）。

- **导入字体文件**：可一次选一个或多个 `ttf / otf / ttc / otc / woff / woff2`，单个文件上限 96MB。字重与字形从文件名推导（`SourceHanSerifSC-Bold.otf` → 族名 `SourceHanSerifSC`、字重 700；`MyFont-LightItalic.ttf` 同时识别出斜体），同一族的多个字重自动并成一行。
- **指定字体文件夹**：整个目录（含子目录）收录为可选用字体，最多 400 个。「重新扫描」拾取之后新增或删除的字体；「清空」移除全部自定义字体（桌面版不会删除磁盘上的字体文件）。
- 收录后字体族会进入上方**原文/译文「字体族」**输入框的候选（标注「已导入字体」），选中即生效；字号与颜色仍按原方式设置。自定义字体缺字时回落到该栏的主题字体（原文=衬线、译文=黑体），不会整段掉字形。
- 存储方式：桌面版 `settings.json` 只记录字体**路径**，不复制字体文件；路径失效时该字体会加载失败并提示。浏览器版把字体数据存在本机 IndexedDB，文件夹模式另外记住目录句柄，重新打开可能需要再授权一次。
- 字体按需加载：只有当前**实际选用**的字体族会被注册，收录几百个字体也不会拖慢启动。
- 字体库的增删即时生效并保存，不依赖「保存外观」；「保存外观」只负责字号、颜色与字体族名。

### 10.5 阅读密度与字数校验口径

「主题」弹窗分两页，三个相关设置项分居其中：

| 设置项 | 位置 | 作用 |
|---|---|---|
| 阅读密度 | 界面氛围 → 03 阅读密度 | **紧凑**（校对扫描、双屏并排）/ **标准**（默认）/ **宽松**（长文精翻、外接大屏） |
| 字数校验口径 | 文本排印 → 02 字数校验口径 | **UTF-8**（中文 3 字节）/ **Shift-JIS**（全角 2 字节，多数 galgame 引擎口径） |
| 每行字数上限 | 文本排印 → 02 字数校验口径 | 按当前口径的字节数计；**留空或填 0 = 关闭超限提示**（指示器隐藏）。默认 120 |

三点注意：

- 密度**只改行高与控件高度**，不改字号、不动 8px 网格。切换立即预览，点「保存外观」才写入 `settings.json` 的 `ui.density`；**取消则还原**。
- 字节口径切换后，译文行右下角指示器的数字与 `UTF8` / `SJIS` 后缀会立即重算。**同一句话两种口径能差出一倍**（如「これはテストです」= 24 字节 UTF-8 / 16 字节 Shift-JIS），务必先确认目标引擎按哪种算。
- 字数**上限**取决于目标引擎的文本框规格（默认 120 是 NScripter 常见值，范围 20–4000）。**口径与上限互不影响**，两项都可以随时改。改上限不需要重启，已显示的行会立即重算颜色与提示。

### 10.6 侧栏

侧栏包含文件、术语和校对三页。打开或关闭侧栏会改变编辑区宽度，工具会重新测量译文框高度。校对页只更新当前可见的批注或修改记录视图。

左侧「当前脚本」卡在文件名与进度文字下方会显示一条**完成度分段条**（与文档标签同款）：进入校对模式前按「已译 / 未译」两段着色，有校对记录后按「已定稿 / 待校对 / 有问题」三段着色，右侧为百分比。

## 11. 保存、导出和进度操作

| 操作 | 实际行为 |
|---|---|
| 保存原文件 | 把当前标准格式写回原文件；桌面版每次保存前把原文件覆盖为 `.bak`（保留上一版），浏览器有句柄时写回，否则另存/下载 |
| 导出译文 | 按「文件」组里的格式下拉导出（见 §11.2）；桌面版选择新路径，浏览器直接下载 |
| 恢复进度 | 把缓存进度合并到当前文件，文件自身已有译文优先 |
| 清空翻译 | 只清正文译文，保留译名；不立即覆盖已保存进度；可撤销 |
| 清除进度 | 删除当前文件缓存进度及校对数据；不清术语、设置或当前内存译文 |

### 11.1 原格式导出规则

「导出译文」选**原格式**时：

- 原文行始终输出。
- `NAME` 始终输出译文行，未改名时保留原名。
- 普通行只有有效译文非空时输出译文行。
- 尽量保留检测到的 LF 或 CRLF。
- 段落结构会被重建，因此非标准输入不保证字节级无损。

### 11.2 其它导出格式

「文件」组的格式下拉除了「原格式」，还有三种**给人看 / 给外部工具用**的产物：

| 格式 | 文件名 | 内容 |
|---|---|---|
| 双语对照 TXT | `<名>.bilingual.txt` | 注释表头（文件名 + 已译/未译条数）+ 每条一段：`[编号] 说话人` / `原：…` / `译：…`；未译显示 `（未译）` |
| 纯译文 TXT | `<名>.zh.txt` | 只输出有译文的行，用于直接贴进引擎；`NAME` 行输出译名（未改则原名） |
| 对照 CSV | `<名>.csv` | 列：`序号,编号,说话人,原文,译文,状态`；带 UTF-8 BOM，Excel 直接打开不乱码 |

三点注意：

- 三种格式都是**单向产物**，不保证能再导回；要回写引擎请用原格式。
- 纯译文格式会**跳过未译行**（留空行等于把原文内容从产物里抹掉）。整篇都没译文时不会导出空文件，会提示改用双语或 CSV。
- `状态` 列：有校对记录时是「已定稿 / 待校对 / 有问题」，否则是「已译 / 未译」。

### 11.3 崩溃恢复

程序在启动时写一份会话标记，正常退出时封存它。如果上次**没有走完退出流程**（崩溃、被任务管理器强杀），下次启动会提示「检测到上次异常退出」，并给出两个选择：

- **恢复进度**：重新打开上次正在编辑的文件，并合并它的进度。
- **从头打开**：忽略这次恢复，按正常流程重新导入。

浏览器版无法保留文件授权（拿不到上次的路径），所以会如实提示"没有可自动重开的文件路径"，进度仍在本地保存 —— 重新导入同一个文件时依然会提示恢复。

### 11.4 历史版本（本地留档）

在写作过程中程序会自动留档，**不需要手动操作**：

| 留档时机 | 标记 |
|---|---|
| 写回原文件前 | `写回原文件前` |
| 批量翻译前 | `批量机翻前` |
| 恢复历史版本前（自动） | `恢复前留档` |

**怎么用**：任意行上点右键 →「历史版本…」打开清单，每项显示时间、留档原因、当时已译行数与「与当前 N 行差异」。选中一条点「恢复该版本」（双击条目也可以）。

**恢复是覆盖式的**，所以二次确认会写清具体后果 —— 例如「将把 37 行改回 2026-09-10 21:30:05 的版本，其中 4 行会被清空译文」。恢复后：

- 可以用 `Ctrl+Z` 撤销；
- 恢复前的内容会**自动再留一份快照**，随时能退回来。

存储位置：源文件同目录的 `.galweave/history/<文件名>/<时间>.json`，每篇最多保留最近 20 份，超出丢弃最旧的。同一次会话内 5 分钟内不重复留档（避免连续保存刷出一堆几乎相同的版本）。

快照**只存有译文的行**（以及改过的译名）—— 空行不存，因为"当时是空"由"不在快照里"表达即可，全篇空行存下来纯属浪费体积。

### 11.5 批量翻译与批量策略

点「批量翻译」会处理所有未翻译的行。执行顺序是：

1. **先查翻译记忆**：精确命中的行直接套用，**不调用机翻**（见 §7.5）。
2. **再走机翻**：按下面的策略翻译剩下的行。
3. **结束后回灌记忆库**，并对受影响的行留一份历史快照。

**批量策略**在「机翻配置 → 采样参数 → 批量策略」设置（两种引擎各自记忆）：

| 策略 | 行为 | 适用 |
|---|---|---|
| **逐行**（默认） | 每行一次请求。某行失败只影响该行 | 最稳，网络/接口不稳时用 |
| **按批次合并请求** | 每 N 行合并成**一次**请求，一次返回多行 | 省往返、省时间；本地模型批量跑时提升明显 |

「每批行数」只在批次模式下生效（2–20）。

**批次模式的安全边界**（重要）：返回值以**行号**对齐，程序要求返回里 `1..N` 每一号都恰好出现一次。只要出现少行、序号重复、或某行的**脚本控制标签**对不上，**这一批整体作废并自动退回逐行重译**，绝不会把 A 行的译文写到 B 行上。批量完成后的提示会分项报账，例如：

```
批量翻译完成：翻译记忆 12 行 · 机翻 143 行 · 批次退回逐行 20 行。
```

括号「」的包裹规则、以及 `<r>` `[n]` `%p…;` 这类行内标签的保护，两种模式完全一致。

**行内标签的两种严格度**（重要）：

| 标签类型 | 举例 | 模型把它弄丢时 |
|---|---|---|
| **命令类** | `%p100;`（定位）、`%f…;`（字体）、未知命令 | **拒绝写入**，提示里会指明是哪一个（如「丢失：定位命令 `%p100;`」）—— 丢命令会改脚本行为，必须人工确认 |
| **换行类** | `<r>`、`[n]`、`[r]`、字面量 `\n` | **自动回填**，不再整行作废。译文断行本来就要随中文长度重排，常需要挪动断点 |

回填位置：按源文顺序插到「下一个还在的标签」之前；若它本来就是本行最后一个标签，则补在行末。模型把标签弄重复或改了顺序仍会拒绝。

注意：真正的换行符（按回车产生的换行）不属于受保护标签，模型去掉它不会触发任何拦截。

**控制字符双向清洗**（传输边界，默认开启，无需设置）：发给模型的文本与模型返回的译文，都会先删掉全部
**控制字符与不可见字符**——换行、回车、制表符、零宽空格、BOM、软连字符、U+2028/2029 行段分隔符、替换符等。
这样做有两个直接好处：单句译文不会夹进看不见的断行；批次模式里某一行原文混入换行时不会再把「序号. 译文」
的对齐冲乱（那是整批作废退回逐行的常见原因）。引擎标签（`<r>` `[n]` `\n` 转义）是普通可见字符，**不在此列**，
仍走上面的保护/回填体系；字面量 `\n` 也刻意不删，避免误伤路径之类的正文。

### 11.6 翻译统计

「更多 → 翻译统计」打开进度面板，回答"还有多久能做完"：

| 指标 | 含义 |
|---|---|
| 已完成 | 百分比 + `已译行数 / 总行数` |
| 剩余 | 还差多少行 |
| 日速度 | 最近记录折算出的「行/天」 |
| 预计完成 | 剩余行数 ÷ 日速度，向上取整成天数与日期 |

下方还有两块：**译文来源**（机翻 / 记忆复用 / 人工各占多少，以及机翻占比）和**最近 14 天**的柱状图（柱高 = 当日新增行数）。

统计的口径，几点需要知道：

- **日速度按日历天摊平**：中断几天再复工，不会把复工那天算成"暴涨"，也不会把没开工的日子算成 0。
- **今天不算进速度**：今天还没过完，拿它算会系统性偏低。
- **推不出速度时如实说**：至少需要两天的记录；刚导入新文件时这一栏是空的。
- 数据按文档分开存（`<源文件目录>/.galweave/stats.json`），每次打开文件、保存原文件、批量翻译、以及正常退出时记一笔。
- **机翻占比**依赖"这行译文是谁写的"：人工输入（含改写机翻结果）计为人工，采纳机翻计为机翻，套用翻译记忆单列。旧项目里已有的译文没有这个标记，会全部算进"人工"。

### 11.7 机翻对比

机翻写进译文时会留一份**基线**；此后你的人工改动都在「机翻 → 定稿」之间算差异。入口在侧栏「校对 → 🤖 机翻对比」。

**能看什么**

- 顶部四个数：有人工改动的行数、直接采纳（一字未改）的行数、**机翻采纳率**、基线条数。
- **改动模式**：把所有改动按可判定的类型归类——标点差异 / 全半角差异 / 只差词尾 / 明显精简 / 明显扩写 / 整句重写，每类附前几条例子。
  这一块是"学习"的部分：如果「标点差异」常年占大头，说明是体例问题，改一遍规则或术语表比逐行改划算；「只差词尾」多，说明语气体例不稳。
- **改动明细**：每行显示 机翻（划掉）→ 定稿，点击跳到该行。

**怎么操作**

- 「用机翻」把该行改回机翻原稿（可 `Ctrl+Z` 撤销）。
- 「刷新」重新读基线并重算。
- 「清空基线」只抹掉参照、**不动译文**（需确认）。

基线存在 `<源文件目录>/.galweave/mtbaseline.json`。重新导入文件（原文变化）后，对不上原文的基线格子会作废，不会把 A 文档的机翻错当成 B 文档的参照。

## 12. 编码与备份

### 12.1 桌面版读取

桌面版依次识别：

- UTF-8 BOM
- UTF-16LE/BE BOM
- 严格 UTF-8
- Shift-JIS
- EUC-JP

Shift-JIS 和 EUC-JP 的判定是启发式，仍可能误判。界面显示的编码文本不应作为唯一判断依据。

### 12.2 浏览器版读取

浏览器版主要支持 UTF-8；非 UTF-8 会尝试 Shift-JIS。它不保证正确处理 UTF-16 或 EUC-JP，旧编码文件请优先使用桌面版并检查结果。

### 12.3 写回编码

两种版本都会把保存/导出内容写为 UTF-8。若游戏引擎必须使用 Shift-JIS，应在保存后用可靠编码工具转换回目标编码，并再次测试。

### 12.4 `.bak`

桌面版每次“保存原文件”前，会把当前文件覆盖为同路径 `.bak`，因此 `.bak` 始终是上一次保存的内容，可用于回退最近一次保存前的版本。只有源文本文件会生成备份，`.proof.json` 与 `glossary.json` 不备份。备份失败不会阻止保存。浏览器版不生成 `.bak`。

## 13. 数据位置和迁移

| 数据 | 桌面版 | 浏览器版 |
|---|---|---|
| 翻译进度 | `<源文件目录>/.galweave/<文件名>.progress.json`（写入失败回退 IndexedDB） | 浏览器 IndexedDB，文件名键 |
| 校对数据 | `<源文件>.proof.json` | IndexedDB |
| 翻译记忆 | `<源文件目录>/.galweave/tm.json` | IndexedDB（键 `tm:<路径或 global>`） |
| 历史版本 | `<源文件目录>/.galweave/history/<文件名>/<时间>.json`（每篇最多 20 份） | IndexedDB |
| 进度统计 | `<源文件目录>/.galweave/stats.json` | IndexedDB |
| 机翻基线 | `<源文件目录>/.galweave/mtbaseline.json` | IndexedDB |
| 项目术语表 | 源文件目录 `glossary.json` | 不支持 |
| 全局术语表 | EXE 同目录 `glossary.json` | 浏览器本地存储 |
| 规则、主题(模式/背景/字体)、校对键、阅读密度、字数口径/上限、查词历史、词典收藏、自定义字体库(路径与元数据) | EXE 同目录 `settings.json` | 浏览器本地存储 |
| 自定义字体的字体数据（浏览器版） | — | IndexedDB（键 `galweave:fontblob:*`，文件夹句柄 `galweave:fontdir`） |
| 会话标记（异常退出检测） | EXE 同目录 `galweave-session.json` | 浏览器本地存储 |
| 主题 | localStorage（旧版一键迁移到 settings.json） | localStorage |

桌面项目迁移时，建议一起复制：

- 源文本。
- 项目目录 `glossary.json`。
- 每个源文本旁的 `.proof.json`。
- **`.galweave/` 目录**（含翻译进度、翻译记忆、历史版本）。
- 如需保留全局设置，再复制 EXE 同目录的 `settings.json` 和全局 `glossary.json`。

只复制文本不会带走进度/记忆/历史 —— 它们都在 `.galweave/` 或 IndexedDB 里。最可靠的跨电脑方式是先“保存原文件”，让译文进入文本，并随项目复制术语、校对与 `.galweave` 目录。

浏览器本地数据受浏览器配置、站点存储和 HTML 打开来源影响。清理浏览器数据、换浏览器或换电脑后可能丢失；应定期保存原文件并导出术语表。

> ⚠️ `.galweave/` 是**项目数据目录，不是缓存**。删掉它会丢失翻译进度、翻译记忆与历史版本。

浏览器本地数据受浏览器配置、站点存储和 HTML 打开来源影响。清理浏览器数据、换浏览器或换电脑后可能丢失；应定期保存原文件并导出术语表。

## 14. 快捷键

| 快捷键 | 功能 |
|---|---|
| `Tab` | 下一行 |
| `Shift+Tab` | 上一行 |
| `Enter` | 下一行 |
| `Ctrl+S` | 保存原文件 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` | 重做 |
| `Ctrl+F` | 聚焦搜索 |
| `F3` | 下一匹配 |
| `Shift+F3` | 上一匹配 |
| `F2` | 下一未翻译 |
| `Ctrl+Enter` | 替换当前匹配 |
| `Ctrl+G` | 聚焦编号跳转 |
| `Ctrl+Shift+P` | 打开命令面板（模糊搜索全部命令） |
| `Q` | 校对：通过并下一行（代码默认） |
| `W` | 校对：有问题（代码默认） |
| `A` | 校对：打开批注（代码默认） |

校对键可自定义，当前发布配置可能与代码默认值不同。

**命令面板**（`Ctrl+Shift+P`）无需记按钮位置：输入几个字即可模糊匹配（如「批量」→ 批量翻译），
`↑`/`↓` 选择、`Enter` 执行、`Esc` 关闭。它**执行的是既有按钮本身**，所以行为与点按钮完全一致，
不会出现"面板里能跑、按钮不认"的分叉；若某个命令在当前版本里对应的按钮不存在，回车会清空查询而不是静默失败。

## 15. 性能和大文件

当前实现会为所有段落创建完整 DOM，并非虚拟滚动。文件切换、搜索、术语和校对已经使用批量挂载、局部刷新、索引和批量布局测量来减少卡顿。

- 数百到约 1000 段属于已实际验证范围。
- 接近 5000 段时 DOM 数量很大，可能出现明显卡顿、内存压力或浏览器失去响应。
- 超大文件建议按章节拆分，完成后再合并。
- 背景图片和侧栏模糊也会增加重绘成本；卡顿时可先清除背景作对比。

## 16. 常见问题

### 导入后乱码

先停止编辑，不要覆盖原文件。用桌面版重新导入；确认文件是纯文本并检查实际编码。如果必须保留旧编码，使用副本并在保存后转换编码。

### 保存后游戏无法读取

最常见原因是游戏要求 Shift-JIS，而工具写成了 UTF-8。从 `.bak`（上一次保存前的原文件）或自有备份恢复，将输出转换为游戏要求的编码后再测试。

### 浏览器版不能保存原文件

Chrome/Edge 需要通过支持文件句柄的选择器打开并授权。普通拖拽、普通文件选择或其他浏览器可能只能另存/下载。不要在不确定当前句柄时反复覆盖，建议使用“导出译文”。

### 为什么出现恢复进度提示

工具检测到该文件键存在已保存的进度。桌面版按**完整路径**（存在 `.galweave/<文件名>.progress.json`）；浏览器版按文件名，因此不同目录的同名文件可能共用提示。恢复时文件内已有译文优先。

### 提示「检测到上次异常退出」，该选哪个

说明上次会话没有走完退出流程（崩溃或被强制结束）。**恢复进度**会重新打开上次的文件并合并进度；**从头打开**则忽略它。选哪个都不会丢东西 —— 进度文件仍在，之后可以手动导入该文件再恢复。浏览器版拿不到上次的文件路径，会提示无法自动重开，这是浏览器不给持久化文件授权的限制。

### 点「历史版本…」说还没有历史版本

历史版本只在**写回原文件**或**批量翻译**时自动留档。另外它有 5 分钟的留档节流：同一篇文档 5 分钟内连续保存只留一份，所以刚打开新文件时列表是空的。浏览器版无法留档（需要能落盘的目录）。

### 翻译记忆没有帮我省下请求

三个常见原因：

1. **记忆库还不是空的判断标准是"源句规范化后完全相等"**，只差一个标点或助词的句子属于"相似命中"，不会自动套用，只会在行右键菜单里给出建议。
2. 源句过短（少于 2 个字符，如「はい」「ええ」）**有意不入库** —— 复用价值低且容易误套。
3. 记忆库随**项目目录**走（`.galweave/tm.json`）。换了目录或文件被移动后，是另一份记忆库。

### 批次翻译比逐行还慢

批次的收益来自减少请求次数，如果每批行数太小（比如 2）、或模型对长输入明显变慢，反而可能更慢。建议本地模型从 5–10 行/批起试。另外看到提示「批次退回逐行 K 行」说明这些批次的返回没能对齐，程序按安全兜底重译了一遍 —— 换更听话的模型或降低每批行数通常能改善。

### 人名没有自动填入

自动应用只处理仍等于原名的译名。检查当前术语表归属和人名键是否与原文完全一致。浏览器版使用全局本地术语表，不读取目录中的 `glossary.json`。

### 词条高亮没有出现

词条匹配区分大小写，且要求精确子串。检查术语原文是否包含多余空格。导入术语后应立即刷新高亮。

### 校对快捷键无法输入英文

默认 `Q/W/A` 在校对模式和译文/译名框聚焦时会触发动作。到“校对 → 快捷键”改为组合键，或暂时关闭校对模式。

### 设置或术语无法保存

桌面版需要 EXE 所在目录可写。把整个发布目录放到普通用户目录，不要直接放在受保护的系统目录。项目术语表还需要源文件目录可写。

### 大文件切换仍卡顿

列表本身走虚拟滚动（只挂视口附近的行），所以"切换文件卡顿"通常另有原因：先清除背景图、关闭校对筛选、收起源代码高亮。若单文件达到数万段仍然慢，属于已知的未加固项（等确定目标规模后再做 benchmark 优化）。

### 如何安全迁移到新电脑

先保存原文件，导出术语表，并复制项目 `glossary.json`、`.proof.json`，以及 **`.galweave/` 目录**（含翻译进度、翻译记忆、历史版本）。`.galweave/` 是项目数据而不是缓存，不要删。浏览器 IndexedDB 和桌面端的 IndexedDB 兜底数据不会随普通文件复制迁移。
