# Galgame 翻译工具 v5.0.0

v5.0.0 将工具从逐行翻译编辑器扩展为面向视觉小说日译中的完整本地化工作台。本版本重点加入真实词典生态、术语与片段效率工具、校对工作流，以及大文件虚拟滚动。

## 主要更新

### 真实词典与划词查询

- 支持本地 **MDX 词典**、galtrans-dict-v1 JSON 词典和自定义 HTTP 词典 API。
- 在原文或译文中选中词语即可查询；多个词典源并发返回结果。
- MDX 支持 `@@@LINK` 变体词跟随、前缀补全和第三方 HTML 安全消毒。
- 桌面版记住 MDX 路径；重启后自动恢复，第一次查词时才懒加载。
- 已验证 Jitendex、大词泉、小学馆日中、新世纪日汉双解、明镜国语辞典等真实词典。

> 目前不解析 MDD 资源包，因此词条文字完整，部分图片、音频和词典自定义图标不显示。

### 术语与快捷片段

- 项目术语表随项目目录保存，支持 JSON / CSV 导入导出。
- CSV 带 UTF-8 BOM，Windows Excel 双击打开不会乱码。
- 快捷片段支持“缩写 → 完整文本”，并与术语共同参与输入建议。
- 输入建议支持 `Tab` 采纳、方向键选择和 `Esc` 关闭，并避开输入法组合输入。

### 大文件编辑体验

- 实现变高行虚拟滚动，只挂载可视窗口附近的行。
- 支持搜索、跳行、下一未翻译、校对过滤和远距离准确定位。
- 聚焦行不会在输入法组合输入过程中被卸载。
- 侧栏支持拖拽调宽、宽度记忆和双击复位。

### 校对与格式识别

- 校对三态：待校对 / 有问题 / 已通过。
- 支持行内批注、批注总览、修改记录、还原、过滤和统计。
- 支持复杂文本格式检测、规范化、编辑器模拟和原格式还原。

### 可靠性

- 桌面版翻译进度保存到 `<源文件>.progress.json`，校对数据保存到 `<源文件>.proof.json`。
- 恢复进度采用合并策略，避免缓存覆盖文件中已经存在的译文。
- 支持 Shift-JIS / EUC-JP 自动检测，保存原文件前自动创建 `.bak`。
- 117 项核心逻辑测试全部通过。

## 下载说明

- `GalgameTranslationTool_5.0.0_x64-setup.exe`：Windows NSIS 安装器（推荐）。
- `GalgameTranslationTool_5.0.0_x64_en-US.msi`：Windows MSI 安装包。
- `galtrans.exe`：免安装可执行文件。
- `GalgameTranslationTool_5.0.0_single.html`：浏览器单文件版（本地 Sakura 机翻不可用，其余核心功能可用）。

## 升级说明

- 可以直接安装覆盖旧版本或使用新的免安装 exe。
- 项目目录中的 `glossary.json`、`snippets.json`、`*.progress.json` 和 `*.proof.json` 不会被安装程序删除。
- 首次在 v5 中添加 MDX 后，桌面版会记住文件路径；请勿随意移动词典文件。
